/**
 * Common Module Index
 * Exports all common utilities
 */

import { EventEmitter, mixinEventEmitter, EventEmitterMixin } from './EventEmitter.js';

import AppSettings from './AppSettings.js';

import { PlaceKeeper, TextNavigation } from './Navigation.js';

export {
  EventEmitter,
  mixinEventEmitter,
  EventEmitterMixin,
  AppSettings,
  PlaceKeeper,
  TextNavigation
};

export default {
  EventEmitter,
  mixinEventEmitter,
  EventEmitterMixin,
  AppSettings,
  PlaceKeeper,
  TextNavigation
};
